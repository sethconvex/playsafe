{
  "name": "agent-sandbox",
  "version": "0.1.0",
  "description": "Run AI coding agents safely using an isolated macOS user and draft-PR-only workflow",
  "bin": {
    "agent-sandbox": "./bin/cli.mjs"
  },
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin/",
    "src/",
    "README.md"
  ]
}
