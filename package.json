{
  "name": "playsafe",
  "version": "0.1.0",
  "description": "Run AI coding agents safely in a sandboxed macOS user with draft-PR-only workflow",
  "bin": {
    "playsafe": "./bin/cli.mjs"
  },
  "type": "module",
  "scripts": {
    "test": "node --test"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "bin/",
    "src/",
    "README.md"
  ]
}
